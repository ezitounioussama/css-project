# CSS Portfolio Page

The HTML portfolio from the previous checkpoint, now styled with an external stylesheet — colours,
typography, spacing, a navbar and a responsive layout. Same content, so the diff between the two
repos is exactly what CSS added.

Sections: hero, About Me, My Work, Resume, Contact Me.

Open `index.html` in any browser — no build step.

| File | What it is |
|---|---|
| `index.html` | The page |
| `styles.css` | The stylesheet |

---

Author: **Oussama Ezitouni**
